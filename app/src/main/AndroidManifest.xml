<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.gin.ginview"
          xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- 从sdcard中读取数据的权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 往sdcard中写入数据的权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".threeD.ThreeDSlid"/>
        <activity android:name=".circleview.CircleActivity"/>
        <activity android:name=".remaindIndexer.RemaindIndexerActivity"/>
        <activity android:name=".autoplay.AutoPlayDemoActivity"/>
        <activity android:name=".banner.BannerViewActivity"/>
    </application>

</manifest>